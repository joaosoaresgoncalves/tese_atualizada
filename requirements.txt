# Reproducing the figures (offline, no API key) needs only the first three.
numpy==2.4.3
matplotlib==3.10.8
# Notebook execution
jupyter==1.1.1
nbformat==5.10.4
nbconvert==7.17.0
ipykernel==6.30.1
# Running the pipeline on the sample (optional; needs an API key)
lxml==5.3.0
openai==1.59.6
python-dotenv==1.0.1
